using System;
using System.Collections.Generic;
using System.Text;
using Xamarin.Forms;

namespace PRMOVIL2CARWASH.Utils
{
    public class NavigationManager
    {
       static Page Page;

        public static void ResetNavigationStack(Page page)
        {
            // Page = page;
            // var _navigation = Page.Navigation;
            //if (_navigation != null && _navigation.NavigationStack.Count() > 0)
            //{
            //    var existingPages = _navigation.NavigationStack.ToList();
            //    foreach (var page in existingPages)
            //    {
            //        _navigation.RemovePage(page);
            //    }
            //}
        }

    }
}
